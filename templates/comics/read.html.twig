{% extends 'base.html.twig' %}

{% block stylesheets %}
    <style>
        body {
            margin: 0;
        }
        img {
            display: block;
            width: 100%;
        }
    </style>
    {{ parent() }}
{% endblock %}

{% block body %}
    {% for file in files %}
        <img src="/chapters/{{ folder }}/{{ file }}"/>
    {% endfor %}
{% endblock %}
